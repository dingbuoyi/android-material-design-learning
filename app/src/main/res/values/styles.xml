<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppBaseTheme" parent="@style/Theme.AppCompat.Light.NoActionBar">
        <!-- customize the color palette -->
        <item name="colorPrimary">@color/bar_color</item>
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:colorPrimary" tools:targetApi="21">@color/purple</item>
        <item name="android:colorPrimaryDark" tools:targetApi="21">@color/purple</item>
    </style>

    <style name="AppTheme.Toolbar.ShadowTitle" parent="TextAppearance.AppCompat.Title">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">30sp</item>
        <item name="android:shadowColor">#40000000</item>
        <item name="android:shadowDy">20</item>
        <item name="android:shadowRadius">10</item>
    </style>

</resources>
